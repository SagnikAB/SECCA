services:
  - type: web
    name: contract-sentinel
    runtime: python
    buildCommand: npm ci && npm run build && pip install -r requirements.txt
    startCommand: gunicorn render_server:app --bind 0.0.0.0:$PORT --workers 1 --threads 4
    healthCheckPath: /
    autoDeploy: true
